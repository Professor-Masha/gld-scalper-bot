import * as THREE from "/static/vendor/three.module.min.js";
export class HudScene {
  constructor(){this.canvas=document.createElement("canvas");this.canvas.className="hud-3d";this.canvas.setAttribute("aria-hidden","true");document.body.prepend(this.canvas);this.scene=new THREE.Scene();this.camera=new THREE.PerspectiveCamera(42,1,.1,100);this.camera.position.set(0,.6,8);this.renderer=new THREE.WebGLRenderer({canvas:this.canvas,alpha:true,antialias:false,powerPreference:"low-power"});this.renderer.setPixelRatio(Math.min(devicePixelRatio||1,1.5));this.group=new THREE.Group();this.scene.add(this.group);this.build();this.pointer={x:0,y:0};this.reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;addEventListener("resize",()=>this.resize());addEventListener("pointermove",e=>{this.pointer.x=(e.clientX/innerWidth-.5)*.18;this.pointer.y=(e.clientY/innerHeight-.5)*.12;},{passive:true});document.addEventListener("visibilitychange",()=>{if(!document.hidden)this.animate();});this.resize();this.animate();}
  build(){const green=new THREE.LineBasicMaterial({color:0x39f49a,transparent:true,opacity:.12}),cyan=new THREE.LineBasicMaterial({color:0x42d9ff,transparent:true,opacity:.09}),ring=new THREE.TorusGeometry(2.9,.018,4,96);for(let i=0;i<4;i++){const mesh=new THREE.LineSegments(new THREE.WireframeGeometry(ring),i%2?cyan:green);mesh.rotation.set(i*.48,i*.72,i*.21);this.group.add(mesh);}this.group.add(new THREE.LineSegments(new THREE.WireframeGeometry(new THREE.IcosahedronGeometry(1.5,2)),cyan));const grid=new THREE.GridHelper(22,22,0x17637a,0x0d2b20);grid.material.transparent=true;grid.material.opacity=.16;grid.position.y=-2.7;this.scene.add(grid);}
  resize(){this.camera.aspect=innerWidth/innerHeight;this.camera.updateProjectionMatrix();this.renderer.setSize(innerWidth,innerHeight,false);}
  animate(){if(document.hidden)return;const speed=this.reduced?0:.0008;this.group.rotation.y+=speed;this.group.rotation.x+=(this.pointer.y-this.group.rotation.x)*.01;this.group.rotation.z+=(this.pointer.x-this.group.rotation.z)*.01;this.renderer.render(this.scene,this.camera);requestAnimationFrame(()=>this.animate());}
}
