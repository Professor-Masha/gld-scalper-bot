# Native Theme Resources

`jarvis.css` defines the black command-center surfaces, green status values,
cyan telemetry, red stop/error controls, chart colors and native table styles.
It is loaded by `JarvisApplication` using the classpath resource path. Layout
dimensions are owned by the Java classes; styling never changes trading policy.
